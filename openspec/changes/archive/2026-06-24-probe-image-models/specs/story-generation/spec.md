## MODIFIED Requirements

### Requirement: Default AI providers are Qwen

The system SHALL default `AppSettings.textProvider` and `AppSettings.imageProvider` to `qwen`, with `textModel` `qwen3.7-plus` and `imageModel` `qwen-image-edit-plus-2025-12-15`. A fresh database SHALL generate real books without manual configuration. Providers and models SHALL remain switchable at runtime via `AppSettings` (an admin MAY set the image model to `wan2.7-image-pro` or `qwen-image-2.0`, or the providers to `gemini`/`stub`).

#### Scenario: Fresh database uses Qwen

- **WHEN** the application initializes its `AppSettings` singleton on a new database
- **THEN** the text and image providers are `qwen` with models `qwen3.7-plus` and `qwen-image-edit-plus-2025-12-15`

#### Scenario: Provider remains switchable

- **WHEN** an admin sets `textProvider`/`imageProvider` to `stub`
- **THEN** subsequent generations use the stub providers without a redeploy

#### Scenario: Image model is switchable

- **WHEN** an admin sets `AppSettings.imageModel` to `wan2.7-image-pro`
- **THEN** subsequent illustrations are generated by that model without a redeploy

## ADDED Requirements

### Requirement: Illustrator accepts multiple reference images

The image generator SHALL accept zero or more reference images for a single
illustration and send each to the provider in one request, so an illustration can be
conditioned on several references (e.g. a hero portrait plus the previous page). The
generator SHALL disable the provider watermark. When no reference image is supplied,
the illustration SHALL be generated from the prompt alone.

#### Scenario: Multiple references in one request

- **WHEN** an illustration is requested with two or more reference images
- **THEN** the generator sends all of them in a single provider request and returns one generated image

#### Scenario: No reference image

- **WHEN** an illustration is requested with no reference image
- **THEN** the generator produces an image from the prompt alone

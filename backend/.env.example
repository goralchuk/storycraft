DATABASE_URL=postgresql://storycraft:storycraft@localhost:5433/storycraft
REDIS_HOST=localhost
REDIS_PORT=6379
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=storycraft
MINIO_SECRET_KEY=storycraft
MINIO_BUCKET=storycraft
PORT=3001

JWT_SECRET=your-jwt-secret-min-32-characters-long
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback
FRONTEND_URL=http://localhost:3000

# Optional — free key from https://aistudio.google.com. Only used when AppSettings.textProvider = "gemini".
GEMINI_API_KEY=

# Alibaba DashScope (Qwen) key. Default provider — needed for real generation (text + images).
QWEN_API_KEY=
